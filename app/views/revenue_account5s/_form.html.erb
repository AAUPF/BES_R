<%= form_with(model: revenue_account5, local: true) do |form| %>
  <% if revenue_account5.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revenue_account5.errors.count, "error") %> prohibited this revenue_account5 from being saved:</h2>

      <ul>
      <% revenue_account5.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Transfer_of_Resources %>
    <%= form.text_field :Transfer_of_Resources %>
  </div>

  <div class="field">
    <%= form.label :Amount %>
    <%= form.text_field :Amount %>
  </div>

  <div class="field">
    <%= form.label :Year %>
    <%= form.text_field :Year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
