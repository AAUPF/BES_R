<%= form_with(model: projection_of_power_requirement4, local: true) do |form| %>
  <% if projection_of_power_requirement4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projection_of_power_requirement4.errors.count, "error") %> prohibited this projection_of_power_requirement4 from being saved:</h2>

      <ul>
      <% projection_of_power_requirement4.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Year %>
    <%= form.text_field :Year %>
  </div>

  <div class="field">
    <%= form.label :Total_Capacity_MW %>
    <%= form.text_field :Total_Capacity_MW %>
  </div>

  <div class="field">
    <%= form.label :Estimated_Peak_Availability_at_State_Periphery_MW %>
    <%= form.text_field :Estimated_Peak_Availability_at_State_Periphery_MW %>
  </div>

  <div class="field">
    <%= form.label :Estimated_Energy_Availability_at_State_Periphery_MU %>
    <%= form.text_field :Estimated_Energy_Availability_at_State_Periphery_MU %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
