<%= form_with(model: health20, local: true) do |form| %>
  <% if health20.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(health20.errors.count, "error") %> prohibited this health20 from being saved:</h2>

      <ul>
      <% health20.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Districts %>
    <%= form.text_field :Districts %>
  </div>

  <div class="field">
    <%= form.label :Institutional_Deliveries %>
    <%= form.text_field :Institutional_Deliveries %>
  </div>

  <div class="field">
    <%= form.label :Year %>
    <%= form.number_field :Year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
