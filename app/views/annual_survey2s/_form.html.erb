<%= form_with(model: annual_survey2, local: true) do |form| %>
  <% if annual_survey2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(annual_survey2.errors.count, "error") %> prohibited this annual_survey2 from being saved:</h2>

      <ul>
      <% annual_survey2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :State %>
    <%= form.text_field :State %>
  </div>

  <div class="field">
    <%= form.label :Cotribution %>
    <%= form.text_field :Cotribution %>
  </div>

  <div class="field">
    <%= form.label :Year %>
    <%= form.text_field :Year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
