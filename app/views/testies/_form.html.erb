<%= form_with(model: testy, local: true) do |form| %>
  <% if testy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testy.errors.count, "error") %> prohibited this testy from being saved:</h2>

      <ul>
      <% testy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
